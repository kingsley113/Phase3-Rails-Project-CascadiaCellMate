<span class="skew-border-box">
<h1 class="fallout-font skew-correction"><%=link_to "Create New Cell", new_cell_path %></h1>
</span>

<span class="skew-border-box ">
<%= form_with url: "/cells", method: :get, class: "skew-correction" do |f| %>
		<%= f.label :query, "Search Cells:" %>
		<%= f.text_field :query%>
		<%= f.submit "Search", class: "submit-button" %>
	<% end %>
</span>

<br><br>

<div>
	<% if @user %>
		<h2><%= @user.display_name %>'s Cells:</h2>
	<% else %>
		<h2>All Cells:</h2>
	<% end %>

	<table id="all-cells">
		<tr>
			<th>Cell Name</th>
			<th>Priority</th>
			<th>% Complete</th>
			<th>Assigned User</th>
		</tr>
		<% @cells.each do |cell| %>
			<tr>
				<td><%=link_to cell.name, cell_path(cell), class: "fallout-font" %></td> 
				<td><%=cell.priority%></td> 
				<td><%=cell.percent_complete%></td> 
				<td>
					<% if cell.user%> 
						<%= cell.user.display_name %> 
					<% else %> 
						--- 
					<% end %></td>
			</tr>
		<% end %>
	</table>
<div>
