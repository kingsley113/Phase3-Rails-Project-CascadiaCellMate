<!DOCTYPE html>
<html>
  <head>
    <title>CascadiaCellMate</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
		<div class="nav-bar">
			<div class="nav-text-italic inline title"> Fallout</div>
			<div class="nav-text-italic inline">
				Cell Mate 
			</div>
			<div class="nav-text-italic inline">
				&nbsp; &nbsp; &nbsp;
				<%=link_to "Home", root_path %> ϟ
				<%=link_to "Cell List", cells_path %> ϟ 
				<%=link_to "Region List", regions_path %> ϟ
				<% if !@user.blank? %>
					<%=link_to "My Cells", user_cells_path(@user) %> ϟ
				<% else %>
					<%=link_to "Sign In", login_path %> ϟ
				<% end %>
				<%=link_to "Quest List", quests_path %> ϟ
				<%=link_to "All Tasks", tasks_path %> 
			</div> 
		</div>

		<div class="padded-body">
			<%= yield %>
		</div>
  </body>
</html>
